# setup-claude-workflow

This command will update the ~/.claude/ folder with my latest content.

```
curl -fsSL https://raw.githubusercontent.com/lautaroblasco23/claude-workflow/main/install.sh | bash
```

After running this curl command, you'll be able to run setup-claude-workflow anywhere.
